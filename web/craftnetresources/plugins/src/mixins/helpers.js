export default {
    methods: {
        /**
         * Returns the Craft ID URL.
         *
         * @returns {String}
         */
        craftIdUrl() {
            return process.env.craftIdUrl
        },
    }
}
